import { Vector3 } from 'three'

const state = {
  activeMesh: null,
  activeMeshName: 'Capot001_CAR_PAINT_0',
  cameraPos: new Vector3(9, 2, 4),
  target: new Vector3(4, 0, 0),
  shouldUpdate: true
}

export default state
